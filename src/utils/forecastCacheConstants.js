/** Misma clave en HomeView y vista de detalle. */
export const FORECAST_STORAGE_KEY = 'forecastById_v1'

/**
 * Subir este número para descartar datos viejos en localStorage
 * (p. ej. tras cambiar la forma del pronóstico o limpiar caché a propósito).
 */
export const FORECAST_CACHE_SCHEMA_VERSION = 3
